<Window x:Class="projectGamesPlatformers.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:projectGamesPlatformers"
        mc:Ignorable="d"
        Title="Авторизация" SizeToContent="Height" Width="450" 
        WindowStartupLocation="CenterScreen" Background="#A8E4A0" ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="MainGreen" Color="#A8E4A0"/>
        <SolidColorBrush x:Key="DarkGreen" Color="#44944A"/>
        <SolidColorBrush x:Key="LightMint" Color="#ADDFAF"/>
        <SolidColorBrush x:Key="OliveBorder" Color="#89AC76"/>

        <Style TargetType="Control" x:Key="InputStyle">
            <Setter Property="Background" Value="#ADDFAF"/>
            <Setter Property="BorderBrush" Value="#89AC76"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,0"/>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#44944A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#89AC76"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White" CornerRadius="15" Margin="40" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.2" ShadowDepth="3"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="ВХОД В СИСТЕМУ" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Foreground="#44944A" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,20"/>

                <TextBlock Text="Логин" Foreground="#89AC76" FontWeight="SemiBold"/>
                <TextBox x:Name="txtLogin" Style="{StaticResource InputStyle}" Margin="0,5,0,15"/>

                <TextBlock Text="Пароль" Foreground="#89AC76" FontWeight="SemiBold"/>
                <PasswordBox x:Name="txtPassword" Style="{StaticResource InputStyle}" Margin="0,5,0,5"/>

                <Button Content="Войти" 
                        Style="{StaticResource PrimaryButton}" 
                        Margin="0,20,0,10" 
                        Click="BtnLogin_Click"/>

                <Button Content="Создать аккаунт" 
                        Click="BtnRegister_Click" 
                        Background="Transparent" 
                        BorderThickness="0" 
                        Foreground="#44944A" 
                        FontSize="11"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
